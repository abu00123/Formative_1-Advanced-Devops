This playbook configures the EC2 server provisioned by Terraform for the **Rwanda Cultural Archives** application.

Install Ansible locally:
   ```bash
   brew install ansible # MacOS
   # or
   pip install ansible
